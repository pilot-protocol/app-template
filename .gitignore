# built binaries (Makefile / `go build` at the repo root)
/pilot-app
/broker
/broker-sign
/publish-server
/maildump
# coverage + generated test output
cover.out
/out/
# never commit publisher signing keys or broker identities
*.key

# browse-tool artifacts
.gstack/
