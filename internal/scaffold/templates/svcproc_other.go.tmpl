//go:build !linux

// Service lifetime for the {{.ID}} cli runner (non-Linux). GENERATED by
// pilot-app; edit pilot.app.yaml and re-generate.
package backend

import "syscall"

// serviceSysProcAttr: there is no parent-death signal outside Linux. The
// server stays in the adapter's process group; StopServices stops it on a clean
// shutdown and the child guard (childguard.go) when the adapter is killed.
func serviceSysProcAttr() *syscall.SysProcAttr { return nil }
