//go:build !linux

package backend

import "syscall"

// childProcAttr: there is no parent-death signal outside Linux. The child stays
// in the adapter's process group, so the supervisor's group-wide stop and its
// stale-instance reaper (kill(-pgid)) still reach it. GENERATED by pilot-app.
func childProcAttr() *syscall.SysProcAttr { return nil }

func lockForChild() func() { return func() {} }
